apiVersion: v1
kind: Service

metadata:
  name: frontend
  namespace: localhelp

spec:
  type: ClusterIP

  selector:
    app: frontend
    purpose: deployment
    tier: frontend-app
    project: localhelp

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080